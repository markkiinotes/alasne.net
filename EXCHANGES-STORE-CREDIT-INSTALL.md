# Exchanges and Store Credit

Install:

```powershell
cd C:\xampp\htdocs\alasne.net
php alasne migrate
composer dump-autoload -o
```

This milestone issues store credit and creates no-charge replacement orders from received returns. Store-credit redemption at checkout is intentionally reserved for the next milestone.
